CREATE TABLE IF NOT EXISTS rosters (
	player text NOT NULL,
    pos text,
    ht text,
    wt text, 
    birth_date text, 
    cntry text,
    experience text,
    college text
    abbrev text PRIMARY KEY
);