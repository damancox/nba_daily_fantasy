CREATE TABLE IF NOT EXISTS schedule (
	dates date NOT NULL,
    start text,
    visitor text,
    pts_v text, 
    home text, 
    pts_h text,
    box_score text,
    ot text,
    attend text,
    notes text
);